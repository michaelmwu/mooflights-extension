{
  "name": "mooflights-extension",
  "version": "0.0.9",
  "description": "MooFlights Google Flights and ITA Matrix companion.",
  "license": "AGPL-3.0-only",
  "private": true,
  "packageManager": "bun@1.3.14",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:dev": "node scripts/build.mjs --dev",
    "build:firefox": "node scripts/build.mjs --browser=firefox",
    "build:firefox:dev": "node scripts/build.mjs --browser=firefox --dev",
    "build:stable": "node scripts/build.mjs --stable-dist",
    "clean": "rimraf dist .context/firefox-build .context/firefox-package-build .context/firefox-stable-package-build",
    "check": "biome check .",
    "dev": "node scripts/build.mjs --watch",
    "dev:stable": "node scripts/build.mjs --watch --stable-dist",
    "format": "biome format --write .",
    "import:mileage-earnings": "node scripts/import-mileage-earnings.mjs",
    "import:airport-coordinates": "node scripts/import-airports.mjs",
    "import:airports": "node scripts/import-airports.mjs",
    "lint": "biome lint .",
    "package": "bun run build && node scripts/package-extension.mjs",
    "package:firefox": "MOOFLIGHTS_DIST_DIR=.context/firefox-package-build bun run build:firefox && MOOFLIGHTS_DIST_DIR=.context/firefox-package-build node scripts/package-extension.mjs --browser=firefox",
    "package:firefox:stable": "MOOFLIGHTS_DIST_DIR=.context/firefox-stable-package-build bun run build:firefox && MOOFLIGHTS_DIST_DIR=.context/firefox-stable-package-build node scripts/package-extension.mjs --browser=firefox --stable-dist",
    "release:next-patch": "node scripts/prepare-next-release-version.mjs --next=patch",
    "release:next-minor": "node scripts/prepare-next-release-version.mjs --next=minor",
    "release:package:next-patch": "bun run release:next-patch && bun run package",
    "release:package:next-minor": "bun run release:next-minor && bun run package",
    "release:verify": "node scripts/verify-release-version.mjs",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:real": "node scripts/run-real-e2e.mjs",
    "typecheck": "tsc --noEmit",
    "webstore:submit": "node scripts/submit-chrome-webstore.mjs"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@playwright/test": "^1.60.0",
    "@types/chrome": "^0.1.34",
    "@types/node": "^25.0.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "esbuild": "^0.28.0",
    "jsdom": "^29.1.1",
    "rimraf": "^6.1.2",
    "typescript": "^6.0.3",
    "vite": "^8.0.14",
    "vitest": "^4.0.16"
  }
}
